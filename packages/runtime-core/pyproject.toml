[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "openrobo-runtime"
version = "0.6.0"
description = "OpenRobo Runtime Verification, Simulation & Connection Inspection Engine"
readme = "README.md"
requires-python = ">=3.10"
license = { text = "Apache-2.0" }
dependencies = [
    "pydantic>=2.6.0",
    "openrobo-schemas",
    "openrobo-workspace",
    "pyyaml>=6.0"
]

[tool.setuptools.packages.find]
where = ["."]

[project.optional-dependencies]
test = ["pytest"]