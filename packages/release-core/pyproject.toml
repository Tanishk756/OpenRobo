[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "openrobo-release"
version = "0.7.2"
description = "OpenRobo Cryptographic Release Packaging, Verification, and Safety Subsystem"
readme = "README.md"
authors = [{ name = "Tanishk Singhal" }]
license = { text = "Apache-2.0" }
requires-python = ">=3.10"
dependencies = [
    "pydantic>=2.0.0",
    "cryptography>=42.0.0",
]

[project.optional-dependencies]
test = [
    "pytest>=8.0.0",
    "pytest-asyncio>=0.23.0",
]
